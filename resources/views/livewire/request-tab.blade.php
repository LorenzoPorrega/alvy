{{-- <div>
  @if ($request)
    <div class="request-details">
      <h2>{{ $request['title'] }}</h2>
      <p>{{ $request['description'] }}</p>
      <!-- Altri dati della request -->
      <button wire:click="closeRequest">Chiudi Request</button>
    </div>
  @else
    <p>Nessuna request selezionata.</p>
  @endif
</div> --}}
<span>Ciao</span>